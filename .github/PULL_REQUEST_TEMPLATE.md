# Summary

<!-- What does this PR change and why? Link related issues with "Closes #123". -->

## Changes

-

## Tests

<!-- How was this verified? `mix precommit` must be green; add/adjust tests for behavior changes. -->

- [ ] `mix precommit` passes (compile --warnings-as-errors, format, tests)
- [ ] `mix credo --strict` clean on changed files
- [ ] Frontend: `npm test` + `tsc --noEmit` (if `frontend/` touched)

## Commit format

Conventional commits: `<type>(<scope>/<subscope>): <description>` — see `CLAUDE.md` for scopes.
